# Cloudflare Runtime Smoke

## Status
- Result: PASS_LOCAL_WORKER_RUNTIME
- Date: 2026-06-04
- Runtime: `wrangler dev .open-next/worker.js --local --host 127.0.0.1 --port 8788`
- OpenNext: `@opennextjs/cloudflare@1.19.11`
- Next.js: `16.2.6`

## Routes
- `/`: 200
- `/robots.txt`: 200
- `/sitemap.xml`: 200
- `/faq`: 200
- `/privacy`: 200
- `/terms`: 200

## API Conversion
- Endpoint: `POST /api/convert`
- Fixture: `tmp/test.png`, 8x8 RGBA PNG generated by `scripts/make-test-png.cjs`
- Result: 200 OK
- Content-Type: `model/stl; charset=utf-8`
- Content-Disposition: `attachment; filename="test.stl"`
- Triangle count header: `320`
- STL first line: `solid pngtostl_relief`
- Facet count in body: `320`

## Error Path
- Non-multipart POST: 400
- Body: `{ "ok": false, "error": "Expected multipart/form-data with a PNG file." }`

## Fixes Made For Runtime Compatibility
- Replaced `pngjs` with `fast-png` because `pngjs` failed to parse valid multipart PNG files in the Cloudflare worker runtime.
- Added `src/types/fast-png.d.ts` because `fast-png` does not expose package `types` through its exports map.
- Updated fixture generation to use `fast-png` encode/decode-compatible PNG data.

## Remaining Runtime Work
- Production deployment smoke is still required after `npm run cf:deploy`.
- Custom domain `pngtostl.net` is not yet verified here.

[NEEDS_REVIEW]
